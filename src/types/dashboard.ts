export type Card = {
  name: string;
  statement: string;
};
export type describe = {
  img: string;
  description: string;
  name: string;
  position: string;
};
export type icons = {
  icon: string;
};
export type img = {
  image: string;
  name: string;
};